{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "description": "Extends basic config with automerge for minor updates",
  "extends": [
    "local>infinite-automations/renovate-configs:default"
  ],
  "packageRules": [
    {
      "matchUpdateTypes": ["minor"],
      "automerge": true
    }
  ]
}
